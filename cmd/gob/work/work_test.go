package work

import (
	"github.com/stretchr/testify/assert"
	"testing"
)

func TestName(t *testing.T) {
	assert.Equal(t, 1, 1)
	//a := modules
	//fmt.Println(mo.TupleToResult(a.Size()).MustGet())
	//fmt.Println(mo.TupleToResult(a.Edges()).MustGet())
	//graph.Tree()
	//graph.DFS(a, root, func(s string) bool {
	//	fmt.Println(s)
	//	return false
	//})
}
