exec:
    commit-msg-hook: ^#[0-9]+:\s*.{10,}$
    pre-commit-hook:
        - list
        - test
    pre-push-hook:
        - list
        - test
plugins:
    html2article:
        alias: lint
        command: run, ./...
        url: golang.org/x/tools/cmd/html2article@v0.16.1
